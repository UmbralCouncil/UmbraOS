# Nix build outputs
result
result-*
*.iso
*.iso.sha256

# Locally packaged release archives
*source-*.tar.gz
umbra-studio-*.tar.zst
attach-iso.sh
umbra-source-*.tar.gz

# Release orchestration lock
.beefcake.lock
/tools/__pycache__/

# Per-machine installer state lives in the managed checkout but is never
# replaced from upstream during an update.
/installer-settings.nix
/updater/target/
